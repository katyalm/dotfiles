
when editing in the middle of a command, it tries to suggest completions (even if they are not real) which breaks the right arrow key, because it is blocked by an invalid autocomplete
also, i want the feature from p10k where when i start typing a command and then use the up arrow, it only searches through commands that start with the prefix i already typed

